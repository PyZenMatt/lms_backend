# Django
DJANGO_SETTINGS_MODULE=schoolplatform.settings
DJANGO_DEBUG=true
SECRET_KEY=change-me

# For Render / production
# Use the production settings module when deploying to Render
DJANGO_SETTINGS_MODULE=schoolplatform.settings.prod

# Postgres (Render provides DATABASE_URL when you link a managed DB)
# DATABASE_URL=postgres://user:pass@host:5432/dbname
# Postgres (Render provides DATABASE_URL when you link a managed DB)
# DATABASE_URL=postgres://user:pass@localhost:5432/schoolplatform

# You can create a local `.env.production` file as a template (already included)
# cp .env.production .env && edit the values for local testing

# Allowed hosts for production (comma separated)
# ALLOWED_HOSTS=your-backend.onrender.com

# Database
# Example: sqlite (dev)
# Using sqlite from backend/db.sqlite3 by default
# Example: Postgres
# DATABASE_URL=postgres://user:pass@localhost:5432/schoolplatform

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# Email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Blockchain (non-sensitive placeholders only)
WEB3_PROVIDER_URL=https://polygon-amoy.g.alchemy.com/v2/your-key
ADMIN_ADDRESS=0xYourAdminAddress
PLATFORM_WALLET_ADDRESS=0xYourPlatformAddress

# Never commit private keys
# ADMIN_PRIVATE_KEY= (do not commit)
# PLATFORM_PRIVATE_KEY= (do not commit)
